 stimParams.spiralSize = app.SpiralSizeSlider.Value;
            stimParams.spiralRevolutions = app.SpiralRevolutionsSpinner.Value;
            stimParams.trigger = app.TriggerDropDown.Value;
            stimParams.triggerCount = app.TriggerCountSpinner.Value;
            stimParams.delay = app.DelaymsEditField_2.Value;
            stimParams.setSize = min(app.NoofTargetsSpinner.Value,nTargets);
            stimParams.duration = app.DurationEditField.Value;
            stimParams.LaserName = app.LaserNameEditField.Value;
            stimParams.power = app.LaserPowerEditField.Value;
            stimParams.IPaddress = app.IPaddressEditField.Value;