axis(app.UIAxes7,'off');
            app.kHzLabel.Visible = 'off';
            app.imTemplate = RTparams.imTemplate;
            app.DFF = RTparams.DFF;
            app.npBWout = RTparams.npBWout; 
            app.rawFluo =  RTparams.rawFluo;
            app.roiBW2 = RTparams.roiBW2;
            app.xcRaw = RTparams.xcRaw;
            app.ycRaw = RTparams.ycRaw; 
            app.coordinates = [RTparams.xcRaw RTparams.ycRaw];
            app.norm_meanIMG = RTparams.norm_meanIMG;
            app.norm_meanRedIMG = RTparams.norm_meanRedIMG;
            app.symmFLAG = RTparams.symmFLAG;
            app.smoothwin = RTparams.smoothwin;
            app.r = RTparams.r;
            app.dffwindow = RTparams.dffwindow;
            app.percent = RTparams.percent;
            app.last_init_frame = RTparams.last_init_frame;
            app.currentFr = RTparams.last_init_frame-999;
            app.displayWin = RTparams.displayWin;
            app.batch_size = RTparams.batch_size;
            app.rank1 = 0;
            app.rank2 = 0; 
            app.rank3 = 0;
            app.select1 = 0;
            app.select2 = 0;
            app.select3 = 0;
            app.selectedCellList = [];
            app.selectedCellsAll = [];
            app.selectedCellFrameID = [];
            app.hidden = [];
            app.gap = RTparams.gap;
            app.exptId = RTparams.exptId;
            app.exptVars = RTparams.exptVars;
            app.filled = RTparams.filled;
            app.minNpSubFluo = RTparams.minNpSubFluo;
            app.maxAdjF = RTparams.maxAdjF;
            app.numInitFrames = RTparams.numInitFrames;
            app.mst = RTparams.mst;
            app.mstWin = RTparams.mstWin;
            app.numAvailFrames = RTparams.numAvailFrames;
            app.thorSyncFile = RTparams.thorSyncFile;
            app.psignalFile = RTparams.psignalFile;
            app.imagingFreq = RTparams.imagingFreq;
            app.rawFileHandle = RTparams.rawFileHandle;
            app.greenChannel = RTparams.greenChannel;
            app.numChannels = RTparams.numChannels;
            app.scope = RTparams.scope;
            app.imgType = RTparams.imgType;
            app.ImageFile = RTparams.ImageFile;
            app.UseacumulativewindowCheckBox.Value = ~(RTparams.mstWin);
            app.selectQueue = [1 2 3]; % First element of this array is the location to put the next selected DF/F trace
            app.JiSignalInfo = RTparams.JiSignalInfo;
            app.redChanDisplayed = 0;
            app.meanIMGhandle = [];
            app.tstack = RTparams.tstack;
            app.xx = [];
            app.yy = [];
            app.topRankedCellIDs = [];
            app.target_cellIDs = [];
            app.selectedID = 0;
            app.loadCorrelated = 0;
            app.loadSelected = 0;
            app.dll_name = RTparams.dll_name;
            app.board_number = RTparams.board_number;
            app.SLM_ON = RTparams.SLM_ON;
            app.SLM = RTparams.SLM;

            app.debugMode = 0; % Testing stimulation response (PriarieLink)
            app.debugIndex = 0;
            app.stimulationParams = [];

%             app.stimShutter = RTparams.stimShutter;
            % Load mean image
            imshow(app.norm_meanIMG,'Parent', app.UIAxes, 'DisplayRange', [0, 0.8]);
            hold(app.UIAxes,'on');